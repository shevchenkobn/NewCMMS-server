in: query
name: select
description: Fields in json you want to consume
schema:
  type: array
  items:
    type: string
    enum:
      - triggerActionId
      - triggerDeviceId
      - actionDeviceId
  uniqueItems: true
  minItems: 1