type: object
required:
  - userId
  - email
  - role
  - fullName
properties:
  userId:
    type: integer
    format: int32
    minimum: 1
  email:
    type: string
    format: email
  role:
    description: Enum actually
    type: integer
    format: int32
    minimum: 1
    maximum: 3
  fullName:
    type: string
additionalProperties: false