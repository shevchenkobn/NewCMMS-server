type: object
required:
  - email
  - role
  - fullName
properties:
  email:
    type: string
    format: email
  role:
    description: Enum actually
    type: integer
    format: int32
    minimum: 1
    maximum: 3
  fullName:
    type: string
  password:
    $ref: ./username-password.yaml
additionalProperties: false