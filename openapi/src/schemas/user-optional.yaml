type: object
properties:
  email:
    type: string
    format: email
  role:
    description: Enum actually
    type: integer
    format: int32
    minimum: 1
    maximum: 3
  fullName:
    type: string
  userId:
    type: integer
    format: int32
    minimum: 1
additionalProperties: false